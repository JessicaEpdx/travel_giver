 <div class="container">
   <h3>Thank you for the generous donation of $<%= @a %> to <%= @project.name  %></h3>
   <%= link_to 'Go back to project', country_project_path(@country, @project) %>
</div>
